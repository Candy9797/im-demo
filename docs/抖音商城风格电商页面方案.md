# 抖音商城风格电商页面方案

仿照抖音商城（Douyin Mall）交互与视觉风格，打造沉浸式、短平快的电商浏览体验。

---

## 一、产品定位

| 维度 | 说明 |
|------|------|
| **风格** | 全屏垂直滑动、单卡片沉浸、强视觉冲击 |
| **场景** | 短视频/直播种草后的快速转化、冲动消费 |
| **核心** | 内容即商品、滑动即浏览、所见即可买 |

---

## 二、技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **框架** | Next.js 15 App Router | 服务端组件、流式渲染、路由 |
| **UI** | React 19 | `use()`、Suspense、并发渲染 |
| **性能** | 虚拟列表、IntersectionObserver、`content-visibility` | 长列表、懒加载、离屏优化 |
| **图片** | next/image | 自动 WebP、懒加载、占位 |
| **样式** | CSS Modules / 内联变量 | 隔离、树摇、主题一致 |
| **状态** | React 19 `useTransition` | 非阻塞交互 |
| **后端** | Node.js + Express（现有） | 可扩展 API |

---

## 三、页面结构

```
┌─────────────────────────────────────┐
│  Header: Logo | 搜索 | 购物车 | 消息   │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  │     Product Card 1          │   │
│  │     (全屏高度，可上下滑动)     │   │
│  │     - 商品图/视频占位          │   │
│  │     - 标题、价格、店铺          │   │
│  │     - 悬浮购买按钮             │   │
│  │                             │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │     Product Card 2          │   │  ← 垂直滑动
│  └─────────────────────────────┘   │
│  ...                               │
├─────────────────────────────────────┤
│  Bottom Bar: 首页 | 商城 | 消息 | 我   │
└─────────────────────────────────────┘
```

---

## 四、核心交互

| 交互 | 实现 |
|------|------|
| **垂直滑动** | CSS `scroll-snap-type: y mandatory` + 每卡片 `scroll-snap-align: start` |
| **懒加载** | `IntersectionObserver` 仅加载可见/临近卡片内容 |
| **购买入口** | 每卡片右下角悬浮「立即购买」「加入购物车」 |
| **快速预览** | 点击卡片进入详情（可后续扩展） |

---

## 五、性能策略

| 策略 | 手段 |
|------|------|
| **虚拟化** | 仅渲染可见区 + 上下各 1 屏缓冲区 |
| **图片** | `loading="lazy"`、`placeholder="blur"`、尺寸适配 |
| **离屏渲染** | `content-visibility: auto` 降低非可见 DOM 成本 |
| **动画** | `transform` + `will-change`，避免 layout |
| **代码分割** | 路由级 lazy、`React.lazy` 拆包 |
| **并发更新** | `useTransition` 包裹非紧急状态更新 |

---

## 六、目录结构

```
src/
├── app/
│   └── shop/
│       ├── page.tsx          # 商城首页
│       └── layout.tsx        # 商城布局（可选）
├── components/
│   └── shop/
│       ├── ShopFeed.tsx      # 垂直滑动商品流
│       ├── ProductCard.tsx   # 单商品卡片
│       ├── ShopHeader.tsx    # 顶部栏
│       └── ShopBottomBar.tsx # 底部 Tab 栏
├── lib/
│   └── shop/
│       └── mockProducts.ts   # Mock 商品数据
└── docs/
    └── 抖音商城风格电商页面方案.md
```

---

## 七、数据模型（Mock）

```typescript
interface Product {
  id: string;
  title: string;
  price: number;        // 分 → 元展示
  originalPrice?: number;
  image: string;
  shopName: string;
  sales?: number;
  tags?: string[];
}
```

---

## 八、访问入口

- **路由**：`/shop`
- **本地**：`http://localhost:3000/shop`（需先 `npm run dev`）

---

## 九、扩展方向

1. **详情页**：`/shop/[id]` 商品详情、规格选择、评价
2. **购物车**：Zustand 管理，与现有 IM 项目解耦
3. **搜索**：顶部搜索框跳转 `/shop/search`
4. **API**：对接 Express 商品列表、分类、推荐接口
5. **视频**：支持商品主图替换为短视频（HLS/MP4）
